name: 'transaction'

pagination:
  default_page_size: 40

relationships:
  category: 'many-to-one'

attributes:
  description: 'VARCHAR(30)'
  value:
    type: 'NUMERIC(9, 2)'
    nullable: false
  date: 'DATE'
